{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "pnpm --filter @repo/db build && pnpm --filter @repo/engine build && tsc -p tsconfig.json",
    "start": "node dist/apps/api/src/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@repo/engine": "workspace:*",
    "@repo/db": "workspace:*",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.44.1",
    "fastify": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}